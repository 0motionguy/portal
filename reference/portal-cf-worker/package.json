{
  "name": "portal-cf-worker",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Reference Portal — Cloudflare Worker. Two routes (GET /portal, POST /portal/call) in one Worker, ~30 lines.",
  "type": "module",
  "main": "./src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:payer": "tsx scripts/test-payer.ts"
  },
  "dependencies": {
    "@visitportal/provider": "workspace:*",
    "@visitportal/spec": "workspace:*",
    "@visitportal/x402-adapter": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "tsx": "^4.20.6",
    "viem": "^2.21.0",
    "vitest": "^2.1.8",
    "wrangler": "^3.114.0"
  }
}
