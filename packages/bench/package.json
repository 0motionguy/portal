{
  "name": "@visitportal/bench",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Reproducible MCP-vs-Portal benchmark. Source of truth for every number on visitportal.dev.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "bench": "tsx scripts/run.ts",
    "bench:smoke": "tsx scripts/smoke.ts",
    "agent-sim": "tsx scripts/agent-sim.ts"
  },
  "dependencies": {
    "@visitportal/provider": "workspace:^0.1.7",
    "@visitportal/visit": "workspace:^0.1.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.20.6",
    "vitest": "^2.1.8"
  }
}
