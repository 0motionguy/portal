# visitportal (Python SDK)

Python visitor SDK for Portal.

**Status:** stretch. Stub only. Phase 3b (Thu Apr 23 PM) mirrors the TS SDK if the TS surface lands on time; otherwise this slot holds for v0.2.

Namespace reserved on PyPI as `visitportal`.
