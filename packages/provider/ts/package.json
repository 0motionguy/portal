{
  "name": "@visitportal/provider",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Optional helper library for Portal providers. Raw HTTP works too — this is for convenience.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"(provider/ts) no tests yet\" && exit 0"
  }
}
