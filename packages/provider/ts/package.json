{
  "name": "@visitportal/provider",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Optional helper library for Portal providers. Stub — planned for v0.2. Raw HTTP (see quickstart-provider.md + reference/trending-demo) works without this.",
  "type": "module",
  "scripts": {
    "test": "echo \"(provider/ts) no tests yet\" && exit 0"
  }
}
