# @visitportal/spec

The Portal spec as code: JSON Schema for `portal.json` manifests plus conformance test vectors.

**Status:** stub — Phase 1 (Apr 20–21) lands `manifest.schema.json`, `conformance/vectors.json`, and `conformance/runner.ts`.

Nothing here yet. See [docs/spec-draft.md](../../docs/spec-draft.md) for the current design.
