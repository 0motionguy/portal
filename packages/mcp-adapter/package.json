{
  "name": "@visitportal/mcp-adapter",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Wrap any MCP server as a Portal. Stub — planned for v0.2. See docs/ROADMAP.md.",
  "type": "module",
  "scripts": {
    "test": "echo \"(mcp-adapter) no tests yet\" && exit 0"
  }
}
