{
  "name": "@visitportal/mcp-adapter",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "description": "Wrap an MCP stdio server as a Portal. Minimal client, manifest translation, and local HTTP bridge.",
  "type": "module",
  "bin": {
    "visitportal-mcp-adapter": "./src/cli.ts"
  },
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "README.md"],
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@visitportal/provider": "workspace:*",
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "vitest": "^2.1.8"
  }
}
