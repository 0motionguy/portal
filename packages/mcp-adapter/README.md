# @visitportal/mcp-adapter

Wrap any MCP server as a Portal. CLI + library.

**Status:** stretch · first to cut. Stub only. Phase 4 (Thu–Fri) lands stdio introspection, schema translation, and a `visitportal-mcp-adapter` CLI — provided Phase 3 (TS SDK) clears on time.

```sh
npx @visitportal/mcp-adapter --mcp "npx some-mcp-server" --port 8080
```
